# Sensitive config (contains real UUID)
xray/config.json

# TLS certificates (privkey is secret)
xray/cert/

# Admin passwords
subscription/.htpasswd
ttyd/.htpasswd

# Subscription config with real UUID
subscription/config.yaml

# Environment files
.env
*.env.local

# OS
.DS_Store
Thumbs.db
