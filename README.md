# Quant Platform Analyse

Public, sanitized snapshot of a private A-share quantitative analysis and backtesting platform.

Excluded from this public snapshot: real `.env` files, databases, market data, generated reports, backups, TLS certificates, htpasswd files, QR codes, real VPN UUID/configuration, and internal run state.
